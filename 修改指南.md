# MAC 完整自動化工具 — 修改指南

這份文件對應拆分後的三個檔案，當你需要微調網站時，可以先在這裡找到「要改什麼、改哪個檔、大概在哪一段」。

---

## 一、檔案結構

| 檔案 | 內容 | 何時要動 |
|------|------|----------|
| `index.html` | 網頁結構（畫面有什麼元素、按鈕、文字） | 改文字、加減按鈕、調整版面結構 |
| `style.css` | 所有外觀樣式（顏色、字體、間距、表格） | 改配色、字型、寬度、間距、手機版 |
| `script.js` | 所有功能邏輯（讀檔、整理資料、產生 Excel） | 改處理規則、欄位、分組方式、下載檔名 |

> 三個檔案放在**同一個資料夾**，直接用瀏覽器打開 `index.html` 即可使用。  
> Excel 讀寫功能由網路上的 SheetJS 函式庫提供（`index.html` 最下方那行 `<script src="https://cdnjs...xlsx...">`），所以使用時需要能連網。

---

## 二、常見微調對照表

### 1. 改文字（標題、說明、按鈕字）
**改 `index.html`**

| 想改的文字 | 搜尋關鍵字 |
|------------|-----------|
| 分頁標題（瀏覽器頁籤） | `<title>` |
| 左上角 Logo | `class="logo"` |
| 首頁大標題 | `class="home-wrap"` 裡的 `<h2>` |
| 首頁說明段落 | `home-wrap` 裡的 `<p>` |
| 流程步驟文字 | `class="flow-step"`（每個 `<h4>` 是標題、`<p>` 是說明） |
| 「開始自動整合處理」按鈕 | `class="start-btn"` |
| 上傳區提示文字 | `class="upload-zone"` 裡的 `<h2>` 和 `<p>` |

### 2. 改顏色 / 主題色
**改 `style.css` 最上方的 `:root` 區塊**

只要改變數值，全站對應顏色就會一起變：

| 變數 | 用途 |
|------|------|
| `--accent` | 主題色（深綠）— 按鈕、高亮、圖示 |
| `--accent-hover` | 滑鼠移上按鈕的顏色 |
| `--accent-light` | 淺底色（卡片標題列、上傳區 hover） |
| `--bg` | 頁面背景 |
| `--text` / `--muted` / `--hint` | 文字由深到淺三種灰 |
| `--danger` | 錯誤訊息的紅色 |

例：要把主題從綠色改成藍色，只改 `--accent`、`--accent-hover`、`--accent-light` 三個值即可。

### 3. 改字型
**改 `style.css` 的 `:root`**：`--sans`（中文內文）、`--mono`（數字/表格）。

### 4. 改版面寬度 / 間距
**改 `style.css`**

| 想改的 | 對應 class |
|--------|-----------|
| 內容區最大寬度 | `.wrap`（`max-width`）、`.home-wrap`（`max-width`） |
| 左右兩欄比例（結果區） | `.result-grid` 的 `grid-template-columns:280px 1fr` |
| 篩選產生器兩欄比例 | `.gen-grid` 的 `grid-template-columns:300px 1fr` |
| 預覽表格最大高度 | `.tbl-wrap` 的 `max-height:480px` |

### 5. 改手機版（窄螢幕）行為
**改 `style.css` 最下方的 `@media(max-width:720px)`**  
這裡控制手機上的內距縮小、雙欄改單欄堆疊。改 `720px` 可調整切換的螢幕寬度門檻。

---

## 三、功能邏輯微調（改 `script.js`）

`script.js` 開頭有分區註解（A 頁面切換 / B 篩選產生器 / C 自動整合 / D 預覽下載），可依下表定位。

### 1. 改「來源工作表」名稱
目前固定找名稱含 `号段分配` 的工作表。  
位置：`processFile` 內 → 搜尋 `号段分配`
```js
const srcName=wb.SheetNames.find(n=>n.includes('号段分配'));
```
把 `'号段分配'` 換成你的工作表名稱即可。

### 2. 改「IMEI / MAC 在哪一欄」
目前 IMEI 取 A 欄（索引 0）、MAC 取 B 欄（索引 1）。  
位置：`processFile` 內
```js
const imeiData=srcData.map(r=>r[0]!=null?String(r[0]):'');           // r[0] = A欄
const macRaw=srcData.slice(1).map(r=>r[1]!=null?String(r[1]):'')...  // r[1] = B欄
```
數字代表欄位索引：A=0、B=1、C=2、D=3…。換欄就改這個數字。

### 3. 改「MAC 每幾筆一組」
目前每 4 筆一組（mac1~mac4）。  
位置：`processFile` 內 → 搜尋 `numCols`
```js
const numCols=4;   // 改這個數字
```
> 注意：若改這裡，下面組資料的 `macGroups.push([...])` 以及產出欄位（mac1~mac4）也要一起調整，否則欄位數會對不上。

### 4. 改「工廠使用」工作表的欄位 / 標題
位置：`processFile` 內 → 搜尋 `IMEI','mac1'`
```js
if(i===0) return ['IMEI','mac1','mac2','mac3','mac4',
  'pcba sn(按照Thinkstart規則生成)','組裝sn(按照Thinkstart規則生成)']; // 標題列
...
return [imei, g[0], g[1], g[2], g[3], '', '']; // 每列資料（最後兩個 '' 是 F、G 欄留空）
```
- 改標題文字 → 改第一行陣列裡的字串。
- F、G 欄（pcba sn / 組裝sn）目前是空的；若要自動填值，把 `return [imei, g[0], g[1], g[2], g[3], '', '']` 最後兩個 `''` 換成你的計算結果。

### 5. 改「新工作表名稱」
目前叫「工廠使用」。位置（`processFile` 內，兩處都要改）：
```js
if(wb.SheetNames.includes('工廠使用')){...}        // 移除舊的
XLSX.utils.book_append_sheet(wb,newWS,'工廠使用');  // 加入新的
```

### 6. 改「下載檔名」
位置：`downloadResult`
```js
XLSX.writeFile(resultWB,`${baseName}_明細表.xlsx`); // baseName 是原檔名
```
篩選範本的檔名則在 `doDownload`：
```js
XLSX.writeFile(wb,`mac_篩選表格${max}.xlsx`);
```

### 7. 改欄寬
位置：`processFile` 內 → 搜尋 `!cols`
```js
newWS['!cols']=[{wch:18},{wch:16},{wch:16},{wch:16},{wch:16},{wch:28},{wch:28}];
// 依序對應 A~G 七欄，wch 數字越大欄位越寬
```

### 8. 改「預覽最多顯示幾列」
位置：`script.js` C 區開頭
```js
const PREVIEW_LIMIT=200; // 改這個數字
```
（只影響畫面預覽，不影響實際輸出的 Excel。）

### 9. 改「篩選產生器預設值」
- 預設數量 N、預設欄位名稱：在 **`index.html`** 的這兩個 input 的 `value`：
  ```html
  <input type="number" id="maxNum" value="3000" .../>
  <input type="text" id="colNames" value="MAC,mac1,mac2,mac3,mac4"/>
  ```
- 預覽最多列數（產生器）：在 **`script.js`** 的 `updateGen` 內 `Math.min(n*dc,16)` 的 `16`。

---

## 四、顯示/隱藏既有區塊

目前有些區塊是刻意隱藏的：

- **導覽列的「篩選表格產生器」按鈕**：在 `index.html` 搜尋 `id="nav-gen"`，移除其中的 `style="display:none;"` 即可讓它顯示。
- **首頁的 Step 1 篩選卡片與向下箭頭**：在 `index.html` 搜尋 `class="flow-card" style="display:none;"` 與 `class="flow-arrow" style="display:none;"`，移除 `style="display:none;"` 即可顯示。

---

## 五、其他注意事項

1. **要連網**：Excel 讀寫靠 SheetJS（`index.html` 底部的 CDN 連結）。若要離線使用，需把該函式庫下載成本地檔案再改成本地路徑。
2. **三個檔名別亂改**：`index.html` 內用 `href="style.css"` 與 `src="script.js"` 連結，若改檔名要同步改這兩處。
3. **改完直接重新整理瀏覽器**即可看到效果，不需要編譯或安裝任何東西。
4. **`setTimeout(...,100)` 是動畫節奏**：`processFile` 裡一層層的延遲只是讓五個步驟動畫依序顯示，不是運算所需；嫌處理太慢可把這些 `100`、`200` 數字調小。
