# 號段分配工具集 — 修改指南

這份文件對應目前的三個檔案，當你需要微調網站時，先在這裡找到「要改什麼、改哪個檔、大概在哪一段」。

> 這份指南已對照**重構後**的版本：`style.css` 是藍圖／派工單風格的新樣式，`script.js` 拆成 `Utils / UI / Router / StackTool / AutoTool` 五個模組。若你手上的檔案是更早版本，行為可能對不上，請先確認三個檔案是同一批。

---

## 一、檔案結構

| 檔案 | 內容 | 何時要動 |
|------|------|----------|
| `index.html` | 網頁結構（首頁、功能①欄位堆疊、功能②MAC自動整合三個頁面） | 改文字、加減按鈕、調整版面結構 |
| `style.css` | 所有外觀樣式（顏色、字體、間距、表格、卡片） | 改配色、字型、寬度、間距、手機版 |
| `script.js` | 所有功能邏輯（讀檔、整理資料、產生 Excel） | 改處理規則、欄位、分組方式、下載檔名 |

> 三個檔案放在**同一個資料夾**，直接用瀏覽器打開 `index.html` 即可使用。
> Excel 讀寫功能由 SheetJS 函式庫提供（`index.html` 開頭 `<head>` 裡那行 `<script src="https://cdnjs...xlsx...">`），所以使用時需要能連網。

這個網站目前是**兩個獨立工具**，沒有共用資料：

- **功能①欄位堆疊**（`page-stack`）：把左右並排的 A/B、C/D 兩組欄位，C、D 接到 A、B 底下，疊成單一兩欄。
- **功能②MAC 自動整合**（`page-auto`）：讀「号段分配」工作表，把 MAC 每 4 筆橫排成 mac1~mac4，產生「工廠使用」工作表。

（若你手上有更早版本 md 提到的「篩選表格產生器」，那是舊版功能，目前這一版的 `index.html`／`script.js` 裡都沒有這段程式碼，若要恢復需要另外加回。）

---

## 二、常見微調對照表

### 1. 改文字（標題、說明、按鈕字）
**改 `index.html`**

| 想改的文字 | 搜尋關鍵字 |
|------------|-----------|
| 分頁標題（瀏覽器頁籤） | `<title>` |
| 左上角 Logo | `class="logo"`（`<span>` 包住的字是強調色那半） |
| 導覽列三個按鈕文字 | `id="nav-home"` / `id="nav-stack"` / `id="nav-auto"` |
| 首頁大標題／說明段落 | `home-wrap` 裡的 `<h2>` 和 `class="lede"` |
| 首頁兩張功能卡片文字 | `class="tool-card is-stack"` / `class="tool-card is-auto"` 裡的 `<h3>`、`<p>` |
| 頁面段落眉標 | 各頁 `class="sec-eyebrow"` |
| 功能①示意圖文字 | `class="stage-label"`、`.col .head`（A·IMEI 等欄名）、`.sch-arrow small` |
| 功能①拖放區文字 | `id="stDrop"` 裡的 `<h3>`、`<p>` |
| 功能①統計卡標籤 | `class="stat imei"` / `class="stat mac"` 裡的 `.k` |
| 功能①下載按鈕 | `id="stDownload"` |
| 功能②上傳區文字 | `id="uploadZone"` 裡的 `<h3>`、`<p>` |
| 功能②處理步驟文字 | `id="ps1"` ~ `id="ps5"` |
| 功能②結果摘要文字 | `class="summary-list"` 裡的 `<span>` |
| 功能②下載／重新上傳按鈕 | `class="btn-green"` / `class="btn-outline"` |
| 功能②預覽表格欄名 | `class="panel"` 裡 `<thead>` 的 `<th>` |

### 2. 改顏色 / 主題色
**改 `style.css` 最上方的 `:root` 區塊**

只要改變數值，全站對應顏色就會一起變：

| 變數 | 用途 |
|------|------|
| `--ink` / `--ink-soft` / `--ink-faint` | 文字由深到淺三種灰藍 |
| `--paper` / `--card` | 頁面背景（含藍圖網格）／卡片底色 |
| `--blueprint` / `--blueprint-deep` / `--blueprint-soft` | 主題深藍 — 導覽列、表頭、按鈕、標題欄 |
| `--teal` / `--teal-soft` / `--teal-deep` | 功能①（欄位堆疊）與「成功／IMEI」語意色 |
| `--amber` / `--amber-soft` / `--amber-deep` | 功能②（MAC自動整合）與「需注意／MAC」語意色 |
| `--red` / `--red-soft` | 錯誤訊息用色 |
| `--accent` | 目前指向琥珀色，功能②頁面眉標用它（`index.html` 裡 `style="--move:var(--accent);"`） |
| `--line` / `--line-strong` | 邊框、分隔線 |

例：想把「功能①＝teal」「功能②＝amber」的語意色對調，改 `--teal*` 和 `--amber*` 那幾個變數即可，不用逐一去改 HTML 上的 class。

### 3. 改字型
**改 `style.css` 的 `:root`**：`--sans`（中文/介面文字，目前是 Noto Sans TC）、`--mono`（所有數字、IMEI、MAC、表格資料，目前是 DM Mono）。
若要換字型，記得同步改 `index.html` `<head>` 裡的 Google Fonts 載入連結。

### 4. 改版面寬度 / 間距
**改 `style.css`**

| 想改的 | 對應位置 |
|--------|-----------|
| 內容區最大寬度 | `.wrap`（`max-width:980px`）、`.home-wrap`（`max-width:900px`） |
| 首頁兩張功能卡片欄數 | `.tool-grid` 的 `grid-template-columns:1fr 1fr` |
| 功能②結果區左右兩欄比例 | `.result-grid` 的 `grid-template-columns:320px 1fr` |
| 功能②預覽表格最大高度 | `.tbl-wrap` 的 `max-height:460px` |
| 功能①統計卡欄數 | `.stats` 的 `grid-template-columns:1fr 1fr` |

### 5. 改手機版（窄螢幕）行為
**改 `style.css` 最下方兩段 `@media`**

- `@media(max-width:860px)`：導覽列換行、兩張首頁卡片／結果區改單欄堆疊。
- `@media(max-width:480px)`：內距再縮小、大標題與統計數字字級再縮小。

改數值可調整切換的螢幕寬度門檻。

---

## 三、功能邏輯微調（改 `script.js`）

`script.js` 由上到下分成幾個區塊：

```
Utils      共用工具（不碰畫面）
UI         共用畫面操作（顯示/隱藏、錯誤提示）
Router     頁面切換
StackTool  功能①欄位堆疊
AutoTool   功能②MAC自動整合
App        進入點 + 向後相容的全域函式
```

每個功能各自的參數都集中放在該模組**最上面幾行**（用大寫常數命名），改參數幾乎都只需要動那幾行，不用再到處找散落的數字。

### 1. 改「來源工作表」名稱（功能②）
目前固定找名稱含 `号段分配` 的工作表。
位置：`AutoTool` 最上面
```js
const SOURCE_SHEET_KEYWORD = '号段分配';
```
改成你的工作表名稱關鍵字即可，只需要改這一處。

### 2. 改「IMEI / MAC 在哪一欄」（功能②）
目前 IMEI 取 A 欄（索引 0）、MAC 取 B 欄（索引 1）。
位置：`AutoTool` 內的 `readImeiAndMac`
```js
const imeiData = rows.map(r => (r[0] != null ? String(r[0]) : ''));
const macRaw = rows.slice(1)
  .map(r => (r[1] != null ? String(r[1]) : ''))
  .filter(v => v);
```
數字代表欄位索引：A=0、B=1、C=2、D=3…，換欄就改這兩個數字。

### 3. 改「MAC 每幾筆一組」（功能②）
位置：`AutoTool` 最上面
```js
const MAC_GROUP_SIZE = 4;   // 改這個數字
```
> 注意：改這裡之後，第 4 點的標題陣列（`mac1`~`mac4`）與 `buildFactoryRows` 裡 `group[0]~group[3]` 的取值、以及第 7 點的欄寬設定也要一起調整，否則欄位數會對不上。

### 4. 改「工廠使用」工作表的欄位 / 標題（功能②）
位置：`AutoTool` 內的 `buildFactoryRows`
```js
if (i === 0) {
  return ['IMEI', 'mac1', 'mac2', 'mac3', 'mac4',
    'pcba sn(按照Thinkstart規則生成)', '組裝sn(按照Thinkstart規則生成)']; // 標題列
}
const group = macGroups[i - 1] || ['', '', '', ''];
return [imei, group[0], group[1], group[2], group[3], '', '']; // 每列資料，最後兩個 '' 是 F、G 欄
```
- 改標題文字 → 改第一段陣列裡的字串。
- F、G 欄（pcba sn／組裝sn）目前是空的；若要自動填值，把最後 `'', ''` 換成你的計算結果。

### 5. 改「新工作表名稱」（功能②）
位置：`AutoTool` 最上面
```js
const OUTPUT_SHEET_NAME = '工廠使用';
```
現在只需要改這**一個常數**——移除舊工作表（`removeSheetIfExists`）和加入新工作表（`book_append_sheet`）都會自動套用這個名稱，不用像以前一樣兩處都手動改。

### 6. 改「下載檔名」
- 功能①：位置在 `StackTool` 的 `handleFile`
  ```js
  outputFilename = Utils.stripExtension(file.name) + '_合併完成.xlsx';
  ```
- 功能②：位置在 `AutoTool` 的 `download`
  ```js
  Utils.downloadWorkbook(state.workbook, `${state.baseName}_明細表.xlsx`);
  ```

### 7. 改欄寬（功能②）
位置：`AutoTool` 內的 `buildFactorySheet`
```js
ws['!cols'] = [{ wch: 18 }, { wch: 16 }, { wch: 16 }, { wch: 16 }, { wch: 16 }, { wch: 28 }, { wch: 28 }];
// 依序對應 A~G 七欄，wch 數字越大欄位越寬
```

### 8. 改「預覽最多顯示幾列」（功能②）
位置：`AutoTool` 最上面
```js
const PREVIEW_LIMIT = 200; // 改這個數字
```
（只影響畫面預覽，不影響實際輸出的 Excel。）

### 9. 改「接受的檔案格式」
- 功能①：`StackTool` 最上面 `const ACCEPTED_EXT = ['xlsx', 'xlsm', 'csv'];`
- 功能②：`AutoTool` 最上面 `const ACCEPTED_EXT = ['xlsx', 'xls'];`

### 10. 改步驟動畫節奏（功能②）
原本每一步都是各自寫 `setTimeout(fn, 100)`，現在統一收在 `Utils.tick(ms = 120)`，`AutoTool` 的 `processFile` 用 `await Utils.tick()` 依序呼叫五次。
- 想全部變快/變慢：改 `Utils.tick` 的預設值 `120`。
- 只想改單一步驟：在對應那行 `await Utils.tick();` 後面加參數，例如 `await Utils.tick(300);`。

### 11. 改錯誤訊息文字
兩個工具的錯誤訊息都是用 `throw new ToolError('文字')` 丟出來的，直接搜尋你想改的那句中文字即可找到位置（例如「找不到「号段分配」工作表」、「C 欄和 D 欄都沒有資料」）。畫面上的提示框兩個功能共用同一種樣式（`.error-box`），改樣式只需要動 `style.css` 一處。

### 12. 改功能①「接合處預覽」要顯示幾筆
位置：`StackTool` 內的 `pickPreviewIndexes`，目前規則是「開頭 3 筆＋接合點前後各 1 筆＋結尾 2 筆」。要調整顯示範圍，改這個函式裡的 `head` / `around` / `tail` 三個陣列即可。

---

## 四、其他注意事項

1. **要連網**：Excel 讀寫靠 SheetJS（`index.html` 開頭的 CDN 連結）。若要離線使用，需把該函式庫下載成本地檔案再改成本地路徑。
2. **三個檔名別亂改**：`index.html` 內用 `href="style.css"` 與 `src="script.js"` 連結，若改檔名要同步改這兩處。
3. **改完直接重新整理瀏覽器**即可看到效果，不需要編譯或安裝任何東西。
4. **HTML 上的 `onclick`／`onchange` 目前都還是 inline 寫法**（例如 `onclick="goPage('stack')"`），`script.js` 最下面的「向後相容層」（`goPage`、`onDragOver`、`downloadResult`…幾個全域函式）就是專門對應這些 inline 呼叫。如果之後要移除 inline handler、改成統一在 JS 裡 `addEventListener` 綁定，記得同步刪掉 `index.html` 上對應的 `onclick`／`onchange`／`ondragover` 等屬性，否則兩邊會對不上或重複觸發。
5. **不要在 `StackTool` / `AutoTool` 以外直接操作它們內部的變數**（例如 `outputWorkbook`、`state.workbook`）——這兩個模組是用 IIFE 包起來的，外部本來就拿不到，這是刻意設計，避免不同模組互相污染狀態。若要跨模組共用資料，建議另外抽一個小小的共用 state 模組，而不是把變數改成全域。
